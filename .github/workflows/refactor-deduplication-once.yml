name: One-off extract deduplication
on:
  push:
    branches: [agent/extract-deduplication]
permissions:
  contents: write
jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/extract-deduplication
          fetch-depth: 0
      - run: python3 tools/_extract_deduplication_once.py
      - run: python3 -m py_compile iptv/deduplication.py iptv/build_core.py build.py
      - run: python3 run_tests.py
      - run: |
          rm tools/_extract_deduplication_once.py
          rm .github/workflows/refactor-deduplication-once.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --stat
          git commit -m "Extract deduplication from build core"
          git push origin HEAD:agent/extract-deduplication
