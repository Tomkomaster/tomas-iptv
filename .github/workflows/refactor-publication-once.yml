name: One-off extract publication
on:
  push:
    branches: [agent/extract-publication]
permissions:
  contents: write
jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/extract-publication
          fetch-depth: 0
      - run: python3 tools/_extract_publication_once.py
      - run: python3 -m py_compile iptv/publication.py iptv/build_core.py build.py
      - run: python3 run_tests.py
      - run: |
          rm tools/_extract_publication_once.py
          rm .github/workflows/refactor-publication-once.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --stat
          git commit -m "Extract publication from build core"
          git push origin HEAD:agent/extract-publication
