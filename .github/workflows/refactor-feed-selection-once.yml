name: One-off extract feed selection
on:
  push:
    branches: [agent/extract-feed-selection]
permissions:
  contents: write
jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/extract-feed-selection
          fetch-depth: 0
      - run: python3 tools/_extract_feed_selection_once.py
      - run: python3 -m py_compile iptv/feed_selection.py iptv/build_core.py build.py
      - run: python3 run_tests.py
      - run: |
          rm tools/_extract_feed_selection_once.py
          rm .github/workflows/refactor-feed-selection-once.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --stat
          git commit -m "Extract feed selection from build core"
          git push origin HEAD:agent/extract-feed-selection
